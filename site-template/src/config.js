// Build-time conference configuration baked in by vite.config.js.
// Values come from site-template/conference.yaml — edit that file,
// then rebuild; do NOT import this at runtime via fetch().
// eslint-disable-next-line no-undef
const conference = __CONFERENCE__

export default conference
